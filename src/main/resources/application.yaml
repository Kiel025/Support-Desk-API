spring:
  application:
    name: SupportDesk API
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  datasource:
    url: jdbc:postgresql://postgres:5432/supportdesk
    username: supportdesk
    password: supportdesk
